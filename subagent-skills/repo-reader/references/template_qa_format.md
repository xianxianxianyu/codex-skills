Purpose: Q&A 的回答格式约束（短、可引用），用于阶段 3 的逐条回答。
When to read: 阶段 3 开始回答前；所有 Mode 都应读。
Inputs: 兴趣点问题列表；Repo Map/Modules/Key Flows 的锚点。
Outputs: N 条 Q&A（数量上限由 Mode 控制），每条尽量引用锚点而非复述。
Do NOT: 不要重复陈述 Repo Map/Modules/Key Flows 的事实；优先写“引用 + 结论”。

# Q&A Answer Format（模板）

每条问题用以下格式（每栏 1–3 行）：

- Question：
- Answer（结论先行）：
- Where to Look（引用锚点：Repo Map / Modules / Key Flows）：
- How to Verify（可选：跑命令/看哪些文件/搜什么关键词）：
- Risks / Trade-offs（可选，1–2 行）：

