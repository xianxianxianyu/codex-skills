# Test Checklist

- Identify the most relevant tests for the change
- Run the smallest meaningful test set first
- Add tests only when behavior changes or bugs are fixed
- Record failures and their fixes
- If tests are skipped, state the reason

